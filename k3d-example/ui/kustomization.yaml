# yaml-language-server: $schema=https://json.schemastore.org/kustomization.json
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ui

secretGenerator:
- name: duckdb-s3
  options:
    disableNameSuffixHash: true
  envs:
  - duckdb-s3-credentials.env
